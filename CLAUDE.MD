## 📋 Правила роботи з Claude Code

### Jupyter Notebook
- **Структура клітинок**: Створювати невеликі, логічні блоки коду, кожен з яких запускається окремо
- **Порядок клітинок**: Клітинки мають бути у логічному порядку виконання (зверху вниз)
- **Тестування**: Спочатку тестувати код у окремих .py файлах, потім створювати чисті клітинки в .ipynb
- **Виконання**: Використовувати `mcp__ide__executeCode` тільки коли notebook відкритий та активний у VS Code

### Робота з бібліотеками
- **xrayutilities**: Основна бібліотека для проекту, повністю працює
- **dtxrd**: Локальна бібліотека з проблемами сумісності scipy (simps→simpson)
- **API помилки**: Завжди перевіряти правильний API перед використанням (Layer vs LayerStack)
- **Помилки сумісності**: Враховувати версії бібліотек та застарілі функції

### Файлова структура
- **Тимчасові файли**: Видаляти test_*.py файли після тестування
- **ANALYSIS.md**: Не змінювати, тільки читати
- **Workspace**: Працювати тільки у папці workspace з .ipynb файлами

---

Я працюю над магістерською роботою.
Це міждисциплінарне дослідження в межах спеціальностей Інженерія програмного забезпечення та Фізика та астрономія.

Тема: **Оцінка якості та точності програмного аналізу X-променевих кривих дифракційного відбивання (XRD)**

Мета: автоматизований підбір структурних параметрів монокристалічних зразків, що забезпечують мінімальне відхилення між експериментальною та теоретичною КДВ (кривою дифракційного відбивання).
Метою даного дослідження є розробка методології застосування згорткових нейронних мереж для аналізу X-променевих кривих дифракційного відбивання та оцінка їх потенціал для автоматизованого визначення структурних параметрів монокристалічних зразків з дефектними приповерхневими шарами.

Основою дослідження є розробка системи генерації синтетичних навчальних даних, що відображають зв'язок між параметрами профілів деформації та відповідними КДВ. Для цього створюється алгоритм системного варіювання ключових параметрів у заданих межах:

·       Dmax1: 0.001 – 0.030 (максимальна деформація в асиметричній гаусіані);
·       D01: 0.002 – Dmax1 (деформація на поверхні в асиметричній гаусіані);
·       L1: 1000 – 7000 Å (товщина порушеного шару в асиметричній гаусіані);
·       Rp1: 0 – L1 (положення максимуму деформації в асиметричній гаусіані);
·       D02: 0.002 – 0.030 (деформація на поверхні в спадній гаусіані);
·       L2: 1000 – 7000 Å (товщина порушеного шару в спадній гаусіані);
·       Rp2: –6000 – 0 Å (положення максимуму деформації в спадній гаусіані);
·       dl: 20 Å (товщина підшарів, константа);
·       Dmin: 0.0001 (мінімальна деформація, константа).

Досліджується приповерхнева область з дефектами, наприклад у наслідок іонної імплантації.

Обовязковий фокус:

1. Визначення параметрів тонких плівок (товщина шару, шорсткість межі, густина тощо) з експериментальних кривих дифракції (XRD). (Це те що ми робимо)
2. Покращення якості даних для структурного аналізу. Окремо варто згадати застосування нейронних мереж для підвищення якості XRD-кривих, що опосередковано допомагає структурному розв’язанню. (Такий підхід нас теж цікавить)

Наукова новизна роботи полягає в розробці програмної моделі на основі машинного навчання для автоматизованого відновлення параметрів структури з X-променевих дифракційних кривих та її інтеграції в існуюче програмне забезпечення з можливістю гібридного використання разом із фізичними моделями.

Це **не XRR у класичному сенсі**, а саме **HRXRD rocking curves (двокристальна дифрактометрія)**.  
Тобто ти працюєш в полі **XRD**, але з вузьким підметодом — аналізом **кривих відбивання монокристалів (rocking curves)**.
У даній роботі під X-променевими кривими дифракційного відбивання маються на увазі rocking curves, отримані методом високороздільної рентгенівської дифрактометрії (HRXRD, double-crystal diffractometry), що відрізняється від X-ray reflectivity (XRR) і традиційного powder XRD.

Моя робота зосереджена на **аналізі двокристальних кривих дифракційного відбивання (HRXRD rocking curves)** від приповерхневих шарів монокристалів з дефектами. Ви прагнете **автоматизовано підбирати структурні параметри**, такі як товщина шару, шорсткість межі, густина, **профілі деформації** та дефекти, враховуючи як когерентну, так і дифузну складові рентгенівського розсіяння

Ти знаходишся у кореневій папці де вже є деякі склоновані проекти з гітхабу.
Їх ти тільки аналізуєш для роботи та береш у використання якісь елементи. Не імпортуєш, а прям береш шматки коду якщо треба

/dtxrd
Wе Python-інструменти для оцінки рентгенівської дифракції **однокристалів**
dtxrd : calculations of reflectivity/transmissivity using the dynamical theory of x-ray diffraction
rcpeak : plotting and statistics of a reflectivity (rocking) curve
rctopo : x-ray rocking curve topography
rctopo-fast : accelerated x-ray rocking curve topography
seehdf : HDF4 and HDF5 image data viewer
specscan : extraction of individual scans from a SPEC data file
throughput : calculations of a throughput and rocking curves of a multicrystal configuration
flux : calculate x-ray flux based on a scaler count rate, a amplifier gain (sensitivity) and a detector responsivity
crl : x-ray compound refractive lens calculator

/xrayutilities
Бібліотека Python, що є потужним інструментом для **аналізу та симуляції даних рентгенівської дифракції**, включаючи тонкі плівки та шаруваті структури.
xrayutilities is a collection of scripts used to analyze and simulate x-ray
diffraction data. It consists of a Python package and several routines coded
in C. For analysis the package is especially useful for the reciprocal space
conversion of diffraction data taken with linear and area detectors. For
simulations code for X-ray reflectivity, kinematical and dynamical diffraction
simulation of crystal truncation rods as well as fundamental parameters powder
diffraction is included.

/autoXRD
В першу чергу орієнтована на **класифікацію кристалічної розмірності та просторової групи** з обмежених наборів даних рентгенівської дифракції тонких плівок.
`autoXRD` **не є прямо застосовною** для Вашого основного завдання, оскільки її функціонал не передбачає кількісного відновлення детальних структурних параметрів або аналізу складних кривих гойдання, що відображають дефекти та профілі деформації в монокристалах. Вона виконує завдання **класифікації**, а не **інверсії** для отримання числових значень фізичних параметрів.
autoXRD is a python package for automatic XRD pattern classification of thin-films, tweaked for small and class-imbalanced datasets. The main application of the package is high-throughput screening of novel materials.
autoXRD performs physics-informed data augmentation to solve the small data problem, implements a state-of-the-art a-CNN architecture and allows interpretation using Average Class Activation Maps (CAMs)

/mlreflect
Ця присвячена **рентгенівській рефлектометрії (XRR)**, яку я чітко відокремлюєте від свого проекту. Однак ця може надати **загальні методологічні ідеї щодо застосування нейронних мереж** для автоматизованого отримання параметрів з даних рентгенівського розсіювання, а також інструменти для роботи з ML в Python (`mlreflect`).
mlreflect - це Python пакет для навчання та використання штучних нейронних мереж, розроблених для аналізу спекулярних рентгенівських та нейтронних рефлектометричних даних. Навчання та використання моделей нейронних мереж здійснюється через Keras як API для TensorFlow. Якщо встановлено, симуляція кривих рефлективності виконується за допомогою C-базованої симуляції пакету refl1D (якщо він не встановлений, використовується вбудована Python-базована симуляція). Інші операції з даними та оптимізації виконуються через пакети numpy та scipy.optimize.
Перевага аналізу даних з використанням нейронних мереж полягає в швидкості отримання результатів, яка зазвичай значно перевищує будь-який типовий алгоритм мінімізації. Це особливо корисно для швидкого скринінгу даних або для аналізу в реальному часі під час експериментів.
Наразі пакет оптимізовано для задач з одношаровими зразками. Після навчання моделі нейронної мережі на конкретному бокс-модельному макеті зразка для заданого діапазону q, вона може передбачати товщину, шорсткість та довжину розсіяння в межах мілісекунд для кривої рефлективності.

Отже з фізичной точки зору під мій проект підходять /dtxrd та /xrayutilities.
А /autoXRD або /mlreflect можуть бути використані для ідей для роботи з кодом та застосування нейронних мереж.

Ми працюємо тільки у папці workspace та з Jupyter Notebook
це значить що ти маєш працювати з .ipynb файлами
